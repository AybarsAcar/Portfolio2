import React from 'react';
import MainActionButton from './MainActionButton';

export default function AboutMe() {
  return (
    <div className="h-screen">
      <MainActionButton text="Button" onClick={() => {}} />
    </div>
  );
}
